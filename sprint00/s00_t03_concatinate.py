##  How to concatenate strings using an operator? What operator is it?

    # the '+' operator is used in concatination in python



## Create a script that:
    # • contains a variable ﬁrst_name with a value 'Monty'
    # • contains a variable last_name with a value 'Python'
    # • contains a variable full_name that must store the result of concatenation of ﬁrst_name and last_name connected with a space ' '
    # • prints the variable full_name

first_name = 'Gris'
second_name = 'Money'
full_name = first_name + ' ' + second_name

print (full_name)
